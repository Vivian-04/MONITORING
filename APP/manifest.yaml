services:
  image: swift-deploy-1-node:latest
  port: 3000
  mode: stable
  version: "1.0.0"


nginx:
  image: nginx:latest
  port: 8080
  proxy_timeout: 30


network:
  name: swiftdeploy-net
  driver_type: bridge

otel:
  exporter_endpoint: http://MONITORING_SERVER_IP:4318
  protocol: http/protobuf
  service_name: swiftdeploy-api

